type: object

required:
  - id
  - name
  - role
  - isApproved
  - email
  - createdAt

properties:
  id:
    type: string
  name:
    type: string
  role:
    $ref: "../enums/role.yml"
  isApproved:
    type: boolean
  email:
    type: string
  createdAt:
    type: string
    format: date-time
